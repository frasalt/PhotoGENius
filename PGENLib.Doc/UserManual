User Manual is still empty.

To generate frames use serial computing script ("./generate-frames.sh")
or parallel computing ("parallel -j NUM_OF_CORES ./generate-frame.sh '{}' ::: $(seq 0 NUM_OF_ANGLES)").
